// Cofiguración para JWT (JSON Web Tokens)
module.exports = {
    secret: 'clave_secreta_videojuegos1234', // clave secreta para firmar tokens
    expiresIn: '1h', // Tiempo de expiración del token
};